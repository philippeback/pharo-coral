accessing
name: aString
	"Name of this parameter in the synopsis. Should reveal its type, role, and multiplicity.
	
	Brackets and punctuation often denote this:
		foo			keyword (typed as is)
		<foo>		meta-syntactic
		foo...		several items of the foo kind
		[foo|bar]	either one
		[foo]		optionally a foo
		[foo...]		zero or more foos"
	
	name := aString